# Módulo Validate.py
# Consigna: Crear las siguientes funciones de validación que serán utilizadas
# por Input.py para verificar los datos ingresados por el usuario.
#
# validate_number(valor, minimo, maximo) -> bool
#   Verifica que el valor numérico esté dentro del rango [minimo, maximo].
#
# validate_length(cadena, longitud_min, longitud_max) -> bool
#   Verifica que la longitud de la cadena esté dentro del rango dado.
#   Utilizar la función len().

def validate_number(valor, minimo, maximo):
    pass

def validate_length(cadena, longitud_min, longitud_max):
    pass
